#include "main.h"

/**
 * _abs - computes absolute value of integer
 * @n: the integer to be computed
 *
 * Return: absulote value of integer
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
